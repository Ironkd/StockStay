# Use modern flag instead of deprecated production config (silences npm warning in containers)
omit=dev
# @adminjs/prisma peers Prisma 5/6; we run Prisma 7 (plan spike — legacy peers accepted)
legacy-peer-deps=true
