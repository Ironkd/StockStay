# Legacy archive — do not run

Scripts and SQL here target the pre-launch **Sale / Inventory** schema (dropped in Prisma migration `20260724000000_drop_legacy_inventory_sale`).

Use **`npx prisma migrate deploy`** against the current schema instead.

Kept only for historical reference.
