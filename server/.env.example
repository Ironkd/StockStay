# Server
PORT=3000
NODE_ENV=development

# Required in production – do not use default secret when deployed
JWT_SECRET=your-super-secret-key-change-this

# Database (Supabase or PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/postgres

# Production: set to your frontend origin (e.g. https://app.stockstay.com)
# Leave unset in development for permissive CORS
# CORS_ORIGIN=https://app.stockstay.com
