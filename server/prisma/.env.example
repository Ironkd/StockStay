# Prisma / database URL (Postgres only — used by migrate & generate when set)
# Local Docker Compose (from repo root: docker compose up -d):
DATABASE_URL="postgresql://stockstay:stockstay@localhost:5433/stockstay"

# Supabase staging or production:
# DATABASE_URL="postgresql://postgres:PASSWORD@db.xxxxx.supabase.co:5432/postgres"
