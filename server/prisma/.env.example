# Database URL for SQLite (default)
DATABASE_URL="file:./dev.db"

# To use PostgreSQL instead, uncomment and update:
# DATABASE_URL="postgresql://user:password@localhost:5432/inventory_db?schema=public"
